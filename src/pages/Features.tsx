import FeatureShowcase from "@/modules/Normal/FeatureShowcase";
import FeaturesList from "@/modules/Normal/FeaturesList";

const Features = () => {
  return (
    <div className="min-h-screen">
      <FeaturesList />
      <FeatureShowcase />
    </div>
  );
};

export default Features;
